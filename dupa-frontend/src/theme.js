"use client";
import { createTheme } from "@mui/material";

const theme = createTheme({
  typography: {
    fontFamily: ["NunitoSans", "Roboto", "sans-serif"].join(","),
  },
});

export default theme;
